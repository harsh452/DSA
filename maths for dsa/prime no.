if we have to check wheather the no. is prime or not just run loop till sqrt(n) not till n;


for(int i=2;i<=sqrt(n);i++){
        if(n%i==0){
            cout<<"not prime";
            return 0;
        }
    }
    cout<<"prime";
    
    or 
    we can say
    int c = 2;
    
    while(c*c<=n){
     if(n%c==0)return false;
    }
    
